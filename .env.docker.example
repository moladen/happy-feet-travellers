# Copy to backend/.env when using Docker from repo root:
#   cp backend/.env.docker.example backend/.env
#
# Then: npm run docker:up

# See backend/.env.docker.example for Postgres, JWT, admin, SMTP, RUN_SEED, etc.

NEXT_PUBLIC_API_URL=http://localhost:5000/api
FRONTEND_PORT=3000
API_PORT=5000
CORS_ORIGINS=http://localhost:3000
